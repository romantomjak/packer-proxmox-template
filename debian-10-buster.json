{
  "variables": {
    "proxmox_host": "10.10.0.10:8006",
    "proxmox_node": "pve-01",
    "proxmox_api_user": "root@pam",
    "proxmox_api_password": "",

    "cores": "2",
    "memory": "2048",
    "disk": "10G",

    "cloud_init_public_ssh_key": ""
  },
  "sensitive-variables": ["proxmox_api_password"],
  "builders": [
    {
      "type": "proxmox",
      "proxmox_url": "https://{{ user `proxmox_host` }}/api2/json",
      "insecure_skip_tls_verify": true,
      "username": "{{ user `proxmox_api_user` }}",
      "password": "{{ user `proxmox_api_password` }}",

      "template_description": "Debian 10 cloud-init template. Built on {{ isotime \"2006-01-02T15:04:05Z\" }}",
      "node": "{{user `proxmox_node`}}",
      "network_adapters": [
        {
          "model": "virtio",
          "bridge": "vmbr0",
          "firewall": true
        }
      ],
      "disks": [
        {
          "type": "scsi",
          "disk_size": "{{ user `disk` }}",
          "storage_pool": "local-lvm",
          "storage_pool_type": "lvm-thin",
          "format": "raw"
        }
      ],
      "scsi_controller": "virtio-scsi-single",

      "iso_file": "local:iso/debian-10.4.0-amd64-netinst.iso",
      "http_directory": "./",
      "boot_wait": "10s",
      "boot_command": [
        "<esc><wait>auto url=http://{{ .HTTPIP }}:{{ .HTTPPort }}/preseed.cfg<enter>"
      ],
      "unmount_iso": true,

      "cloud_init": true,
      "cloud_init_storage_pool": "local",

      "vm_name": "debian-10.4.0-amd64",
      "memory": "{{ user `memory` }}",
      "cpu_type": "EPYC",
      "sockets": "1",
      "cores": "{{ user `cores` }}",
      "os": "l26",

      "ssh_username": "root",
      "ssh_password": "toor"
    }
  ],
  "provisioners": [
    {
      "type": "file",
      "source": "cloud.cfg",
      "destination": "/etc/cloud/cloud.cfg"
    },
    {
      "type": "shell",
      "inline": ["sed -e 's^PACKER_CLOUD_INIT_PUBLIC_SSH_KEY^{{ user `cloud_init_public_ssh_key` }}^' -i /etc/cloud/cloud.cfg"]
    }
  ]
}
